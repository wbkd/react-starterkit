import React from 'react';
import { Link } from 'react-router';

const Footer = (props) => {
    return (
        <footer className="clearfix">
            Website info.
        </footer>
    )
};

export default Footer;